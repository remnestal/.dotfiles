[user]
		email = albin.remnestal@gmail.com
		name = Albin Remnestål

[core]
		editor = vim

[push]
		default = matching
		
[pull]
		ff = only	

[color]
		diff = auto
		status = auto
		branch = auto
		ui = true

[color "status"]
		added = green
		changed = yellow
		untracked = red

[pretty]
		florida = format:%C(yellow)%h %C(blue)%<(10)(%cr) %C(green)%<(7)%aN %Creset%s%Cred%d
